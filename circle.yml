machine:
    java:
      version:
        oraclejdk8

test:
    override:
        - mvn test
    post:
        - mvn jacoco:report
        - mvn com.gavinmogan:codacy-maven-plugin:coverage -DcoverageReportFile=/target/report/jacoco/jacoco.xml -DprojectToken=$CODACY_PROJECT_TOKEN -DapiToken=$CODACY_API_TOKEN

general:
    branches:
      only:
        - master
