machine:
    java:
      version:
        oraclejdk8

test:
    override:
        - mvn test
    post:
        - mvn jacoco:report
        - mvn com.gavinmogan:codacy-maven-plugin:coverage -DcoverageReportFile=target/report/jacoco/jacoco.xml -DprojectToken=f6122739cc1c49d8bc7c024833691a6e -DapiToken=blah

general:
    branches:
      only:
        - master
